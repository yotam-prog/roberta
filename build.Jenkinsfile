pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'ls'
                sh 'echo "Hello World"
                sh 'echo building...'
            }
        }
    }
}
